apiVersion: 1

deleteDatasources:
  - name: Prometheus
    orgId: 1

datasources:
- name: Prometheus
  type: prometheus
  typeName: Prometheus
  access: proxy
  url: http://prometheus:9090
  password:
  user:
  basicAuth: false
  isDefault: true
  jsonData:
    httpMethod: POST
  readOnly: false
